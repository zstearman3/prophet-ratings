<!DOCTYPE html>
<html>
  <head>
    <title>ProphetRatings</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload", skip_pipeline: Rails.env.test? %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module", skip_pipeline: Rails.env.test? %>
  </head>

  <body>
    <%= render "layouts/navbar" %>
    <main class="container mx-auto mt-6 px-5">
      <%= yield %>
    </main>
  </body>
</html>
